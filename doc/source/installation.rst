============
Installation
============

At the command line::

    $ pip install coverage2sql

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv coverage2sql
    $ pip install coverage2sql
